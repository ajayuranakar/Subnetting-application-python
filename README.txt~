This is a simple python application which takes valid IP address(IP v4), subnet mask as input and if values entered are invalid then app requests user to re-enter the fields. This application will give network address, broadcast address, number of hosts in the network and it also gives an option for the user to generate a random IP address. This application can be used as a part of DHCP server.
